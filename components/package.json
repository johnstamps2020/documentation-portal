{
  "name": "@doctools/components",
  "version": "1.0.6",
  "description": "A collection of React components for Doctools",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "publishConfig": {
    "registry": "https://artifactory.guidewire.com/api/npm/doctools-npm-dev/"
  },
  "scripts": {
    "build": "rimraf dist && tsc --build --verbose && copy-non-tx-files",
    "dev": "yarn build && tsc --watch"
  },
  "devDependencies": {
    "@doctools/scripts": "workspace:*",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@doctools/server": "workspace:*"
  },
  "peerDependencies": {
    "@emotion/react": "^11.11.0",
    "@emotion/styled": "^11.11.0",
    "@fontsource/source-sans-pro": "^4.5.11",
    "@mui/icons-material": "^5.15.6",
    "@mui/material": "^5.15.6",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
