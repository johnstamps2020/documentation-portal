:::danger Not secure

This is **not a way to hide sensitive information**. The content is still sent
to the browser and savvy users can get to it if they want. We use this component
to hide content which is not relevant to external users to avoid confusing them.

:::
