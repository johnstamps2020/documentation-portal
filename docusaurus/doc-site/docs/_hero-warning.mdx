:::note

The title renders an H1 element, so remember to set `hide_title` to `true` in
your frontmatter. However, you still need t set the title somewhere, either in
the file body, the frontmatter, your nav file, or the category meta file.

```mdx
---
hide_title: true
---

# Your title here
```

:::
